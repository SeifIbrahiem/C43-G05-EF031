namespace C43_G05_EF031
{
    internal class Program
    {
        static void Main(string[] args)
        {
            #region Entity frame work core 
            //orm tool in dotnet
            //orm <> object relation mapper 
            //backend app <> 
            //orm <App---Db>
            //Importance with EFCORE
            //Mapping
            //code first : generate table per each class
            //DB  first :  generate class per each table

            //l2e(linq to entity)//crud
            //create -- read -- update -- delete 

            //TCP : Table per class
            //TBH : Table per hierachy
            //Tpcc: Table per concert class

            //<><><><><><><><><><><><><><><><><><><><<><><><>//
            //(ADO / EFCORE / DOPPER )
            //the choice of tool depends on <the requirement of your projet>
            //if you need light weight and fast  (dapper)
            //if you need height level api and many features (ef core)
            //if you need fine grained control overdata base operation (ado.net)
            #endregion
        }
    }
}
